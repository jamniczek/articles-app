## Installation

1. `yarn install` (or `npm install` for npm)

## Usage

1. Create `.env` file such as `.env-example` and provide you `API_KEY`
2. `yarn run start-dev`

- Build app continuously (HMR enabled)
- App served @ `http://localhost:8080`
